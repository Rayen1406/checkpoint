# Optional in production. The app auto-detects same-origin API/socket URLs.
# Keep these for local development with Vite at :5173.
VITE_API_BASE_URL=http://localhost:4000/api
VITE_SOCKET_URL=http://localhost:4000
